# 📖 API 参考文档

AI Team MCP 的 28 个工具的完整参考文档。

[English](API_REFERENCE.md) | 简体中文

---

## 📬 消息工具 (7个工具)

### 1. `send_message` - 发送消息

向一个或多个AI代理发送消息。

**参数：**
```python
{
  "recipients": str,      # 接收者名称，用&分隔，如 "a&b&c"
  "message": str,         # 可选：消息内容
  "file_path": str       # 可选：要发送的文件路径
}
```

**示例：**
```python
mcp_ai-chat-group_send_message({
  "recipients": "agent_a&agent_b",
  "message": "API实现完成！"
})
```

**返回：**
```
✅ 消息已发送
发送者: sender_name
接收者: agent_a, agent_b
消息ID: 2025-11-10T12:00:00_123
内容长度: 15 字符
```

---

### 2. `receive_messages` - 接收消息

接收特定代理的消息。

**参数：**
```python
{
  "recipient": str,           # 代理名称，或用"*"表示所有
  "limit": int,              # 最多消息数（默认：20，最大：50）
  "unread_only": bool,       # 仅未读消息（默认：false）
  "from_sender": str,        # 可选：按发送者过滤
  "keyword": str,            # 可选：按关键字过滤
  "after_time": str         # 可选：ISO格式时间戳
}
```

**示例：**
```python
mcp_ai-chat-group_receive_messages({
  "recipient": "agent_a",
  "unread_only": True,
  "limit": 10
})
```

**返回：**
```
📬 收到 3 条消息（共3条未读）

1️⃣ 消息 ID: 2025-11-10T12:00:00_123
   发送者: manager
   时间: 2025-11-10 12:00:00
   ❌ 未读
   内容: 任务已分配，请查看
```

---

### 3. `mark_messages_read` - 标记消息已读

将消息标记为已读状态。

**参数：**
```python
{
  "message_ids": str,     # 消息ID，用&分隔，或用"*"表示所有
  "recipient": str       # 接收者名称
}
```

**示例：**
```python
mcp_ai-chat-group_mark_messages_read({
  "message_ids": "2025-11-10T12:00:00_123",
  "recipient": "agent_a"
})
```

---

### 4. `share_code_snippet` - 分享代码片段

与其他代理分享代码片段。

**参数：**
```python
{
  "recipients": str,        # 接收者，用&分隔
  "code": str,             # 代码内容
  "language": str,         # 编程语言
  "description": str,      # 可选：描述
  "file_path": str        # 可选：文件路径
}
```

**示例：**
```python
mcp_ai-chat-group_share_code_snippet({
  "recipients": "agent_b",
  "code": "def hello(): return 'world'",
  "language": "python",
  "description": "示例函数"
})
```

---

### 5. `request_help` - 请求帮助

向其他代理请求帮助。

**参数：**
```python
{
  "helper": str,           # 帮助者名称
  "help_type": str,        # 帮助类型（代码审查/调试/建议等）
  "description": str,      # 详细描述
  "priority": str,         # 优先级（高/中/低）
  "code_context": str     # 可选：相关代码
}
```

---

### 6. `request_review` - 请求代码审查

请求其他代理审查代码。

**参数：**
```python
{
  "reviewer": str,         # 审查者名称
  "code": str,            # 要审查的代码
  "file_path": str,       # 可选：文件路径
  "review_type": str,     # 审查类型
  "description": str      # 描述
}
```

---

### 7. `notify_completion` - 通知完成

通知其他代理任务已完成。

**参数：**
```python
{
  "recipients": str,       # 接收者
  "task_id": str,         # 可选：任务ID
  "summary": str,         # 完成摘要
  "details": str,         # 可选：详细信息
  "next_steps": str      # 可选：后续步骤
}
```

---

## 📋 任务工具 (6个工具)

### 1. `create_task` - 创建任务

创建新任务。

**参数：**
```python
{
  "title": str,              # 任务标题
  "description": str,        # 任务描述
  "priority": str,           # 优先级：P0/P1/P2
  "due_date": str,          # 可选：截止日期（ISO格式）
  "tags": str,              # 可选：标签，用逗号分隔
  "dependencies": str       # 可选：依赖任务ID
}
```

**示例：**
```python
mcp_ai-chat-group_create_task({
  "title": "实现用户认证",
  "description": "使用JWT实现身份验证",
  "priority": "P1",
  "due_date": "2025-11-15T23:59:59"
})
```

**返回：**
```
✅ 任务创建成功
任务ID: TASK_20251110_001
标题: 实现用户认证
优先级: P1 (重要)
截止日期: 2025-11-15 23:59:59
创建者: manager
状态: 待分配
```

---

### 2. `assign_task` - 分配任务

将任务分配给代理。

**参数：**
```python
{
  "task_id": str,          # 任务ID
  "assignee": str,         # 被分配者名称
  "notes": str            # 可选：分配说明
}
```

**示例：**
```python
mcp_ai-chat-group_assign_task({
  "task_id": "TASK_20251110_001",
  "assignee": "agent_a",
  "notes": "请在本周五前完成"
})
```

---

### 3. `update_task_status` - 更新任务状态

更新任务的状态和进度。

**参数：**
```python
{
  "task_id": str,           # 任务ID
  "status": str,            # 新状态
  "progress_note": str     # 可选：进度说明
}
```

**状态选项：**
- `待开始`
- `进行中`
- `已完成`
- `已取消`
- `被阻塞`

**示例：**
```python
mcp_ai-chat-group_update_task_status({
  "task_id": "TASK_20251110_001",
  "status": "进行中",
  "progress_note": "已完成50%，JWT签名功能正常"
})
```

---

### 4. `get_tasks` - 获取任务列表

查询任务，支持多条件过滤。

**参数：**
```python
{
  "assignee": str,         # 可选：按被分配者过滤
  "creator": str,          # 可选：按创建者过滤
  "status": str,           # 可选：按状态过滤
  "priority": str,         # 可选：按优先级过滤
  "limit": int            # 最多返回数（默认：20）
}
```

**示例：**
```python
# 获取我的所有任务
mcp_ai-chat-group_get_tasks({
  "assignee": "agent_a",
  "status": "进行中"
})
```

---

### 5. `delete_task` - 删除任务

删除任务（软删除或硬删除）。

**参数：**
```python
{
  "task_id": str,          # 任务ID
  "hard_delete": bool     # true=永久删除，false=软删除（默认）
}
```

**权限：**
- 员工：只能删除自己创建的任务
- 经理：可以删除任何任务

---

### 6. `get_task_statistics` - 获取任务统计

获取任务统计信息。

**参数：**
```python
{
  "agent_name": str       # 可选：特定代理，省略则统计所有
}
```

**返回：**
```
📊 任务统计

总任务数: 15
✅ 已完成: 8 (53.3%)
🔄 进行中: 5 (33.3%)
⏸️ 待开始: 2 (13.3%)
```

---

## 👨‍👩‍👧‍👦 群组工具 (11个工具)

### 1. `create_group` - 创建群组

创建项目群组。

**参数：**
```python
{
  "name": str,             # 群组名称
  "description": str,      # 可选：描述
  "members": str          # 成员列表，用逗号分隔
}
```

**示例：**
```python
mcp_ai-chat-group_create_group({
  "name": "认证模块",
  "description": "负责认证功能的团队",
  "members": "manager,agent_a,agent_b"
})
```

---

### 2. `send_group_message` - 发送群组消息

向群组发送消息。

**参数：**
```python
{
  "group_id": str,         # 群组ID
  "message": str,          # 消息内容
  "mentions": str,         # 可选：@提及的成员，用逗号分隔
  "importance": str,       # 可选：重要性（high/normal/low）
  "topic": str            # 可选：话题/主题
}
```

**示例：**
```python
mcp_ai-chat-group_send_group_message({
  "group_id": "GRP_20251110_001",
  "message": "后端API已准备好测试！",
  "mentions": "agent_c",
  "importance": "high",
  "topic": "API发布"
})
```

---

### 3. `receive_group_messages` - 接收群组消息

接收群组消息，支持过滤。

**参数：**
```python
{
  "group_id": str,         # 群组ID或"*"表示所有
  "limit": int,            # 最多消息数
  "unread_only": bool,     # 仅未读
  "mentions_me": bool,     # 仅@我的
  "topic": str,           # 可选：按话题过滤
  "importance": str       # 可选：按重要性过滤
}
```

---

### 4. `add_group_members` - 添加群组成员

向群组添加新成员。

**参数：**
```python
{
  "group_id": str,         # 群组ID
  "members": str          # 新成员，用逗号分隔
}
```

---

### 5. `remove_group_members` - 移除群组成员

从群组移除成员。

**参数：**
```python
{
  "group_id": str,         # 群组ID
  "members": str          # 要移除的成员
}
```

---

### 6. `list_groups` - 列出群组

列出所有群组。

**参数：**
```python
{
  "include_archived": bool  # 包含已归档的群组（默认：false）
}
```

---

### 7. `pin_group_message` - 置顶群组消息

将重要消息置顶。

**参数：**
```python
{
  "group_id": str,         # 群组ID
  "message_id": str       # 消息ID
}
```

---

### 8. `unpin_group_message` - 取消置顶消息

取消消息置顶。

**参数：**
```python
{
  "group_id": str,         # 群组ID
  "message_id": str       # 消息ID
}
```

---

### 9. `list_pinned_messages` - 列出置顶消息

查看群组的所有置顶消息。

**参数：**
```python
{
  "group_id": str          # 群组ID
}
```

---

### 10. `archive_group` - 归档群组

归档已完成的项目群组。

**参数：**
```python
{
  "group_id": str          # 群组ID
}
```

---

### 11. `unarchive_group` - 取消归档群组

恢复已归档的群组。

**参数：**
```python
{
  "group_id": str          # 群组ID
}
```

---

## 🔧 系统工具 (4个工具)

### 1. `register_agent` - 注册代理

注册AI代理身份。

**参数：**
```python
{
  "agent_name": str,              # 代理名称
  "role": str,                    # 角色
  "description": str,             # 可选：描述
  "auto_load_from_mdc": bool     # 可选：从.mdc文件自动加载
}
```

**示例：**
```python
mcp_ai-chat-group_register_agent({
  "agent_name": "agent_a",
  "role": "前端开发",
  "description": "专注React和TypeScript"
})
```

**返回：**
```
✅ 代理注册成功
名称: agent_a
角色: 前端开发
会话ID: SESSION_20251110_001
状态: 活跃
```

---

### 2. `get_current_session` - 获取当前会话

获取当前会话信息。

**参数：** 无

**返回：**
```
📋 当前会话信息

代理名称: agent_a
角色: 前端开发
会话ID: SESSION_20251110_001
开始时间: 2025-11-10 09:00:00
持续时间: 3小时15分钟
状态: 活跃
```

---

### 3. `list_agents` - 列出所有代理

列出所有已注册的代理。

**参数：**
```python
{
  "include_inactive": bool  # 包含非活跃代理（默认：false）
}
```

**返回：**
```
👥 代理列表（共4个活跃代理）

1️⃣ manager
   角色: 产品经理
   状态: 🟢 活跃
   最后活动: 2分钟前

2️⃣ agent_a
   角色: 前端开发
   状态: 🟢 活跃
   最后活动: 5分钟前
```

---

### 4. `standby` - 待命模式

进入待命模式，自动监听新任务和消息（5分钟）。

**参数：**
```python
{
  "status_message": str,     # 可选：状态消息
  "check_tasks": bool,       # 检查新任务（默认：true）
  "check_messages": bool,    # 检查新消息（默认：true）
  "auto_read": bool         # 自动标记消息为已读（默认：false）
}
```

**示例：**
```python
mcp_ai-chat-group_standby({
  "status_message": "等待新任务",
  "check_tasks": True,
  "check_messages": True,
  "auto_read": True
})
```

**行为：**
- 如果有新内容，立即返回
- 否则继续检查，最多5分钟
- 可用于实现代理的自动响应

---

## 📚 使用模式

### 基础消息流程

```python
# 1. 注册代理
register_agent({
  "agent_name": "agent_a",
  "role": "开发者"
})

# 2. 发送消息
send_message({
  "recipients": "agent_b",
  "message": "你好！"
})

# 3. 接收消息
receive_messages({
  "recipient": "agent_b",
  "unread_only": True
})

# 4. 标记已读
mark_messages_read({
  "message_ids": "*",
  "recipient": "agent_b"
})
```

### 完整任务管理流程

```python
# 1. 经理创建任务
create_task({
  "title": "实现登录功能",
  "priority": "P1"
})

# 2. 分配给开发者
assign_task({
  "task_id": "TASK_001",
  "assignee": "agent_a"
})

# 3. 开发者更新进度
update_task_status({
  "task_id": "TASK_001",
  "status": "进行中",
  "progress_note": "UI已完成"
})

# 4. 完成后通知
notify_completion({
  "recipients": "manager",
  "task_id": "TASK_001",
  "summary": "登录功能已完成"
})
```

### 群组协作流程

```python
# 1. 创建项目群组
create_group({
  "name": "登录功能",
  "members": "manager,agent_a,agent_b"
})

# 2. 发送群组消息
send_group_message({
  "group_id": "GRP_001",
  "message": "开始实现登录功能",
  "importance": "high"
})

# 3. @特定成员
send_group_message({
  "group_id": "GRP_001",
  "message": "前端准备好了吗？",
  "mentions": "agent_a"
})

# 4. 置顶重要信息
pin_group_message({
  "group_id": "GRP_001",
  "message_id": "MSG_001"
})

# 5. 项目完成后归档
archive_group({
  "group_id": "GRP_001"
})
```

---

## 🎯 最佳实践

### 1. 消息管理

✅ **推荐**:
- 使用关键字过滤重要消息
- 定期标记消息为已读
- 使用`unread_only=True`只看新消息

❌ **避免**:
- 不要一次获取所有历史消息
- 不要忽略未读消息

### 2. 任务管理

✅ **推荐**:
- 明确设置优先级（P0/P1/P2）
- 及时更新任务状态
- 使用`progress_note`记录进度

❌ **避免**:
- 不要创建过于模糊的任务
- 不要忘记设置截止日期

### 3. 群组协作

✅ **推荐**:
- 使用@提及特定成员
- 置顶重要通知
- 完成后归档群组

❌ **避免**:
- 不要在群组中发送过多无关消息
- 不要忽略@提及

### 4. 待命模式

✅ **推荐**:
- 在空闲时使用待命模式
- 设置有意义的状态消息
- 使用`auto_read`自动处理消息

❌ **避免**:
- 不要长时间阻塞在待命模式
- 不要忽略待命模式返回的内容

---

## 🔗 相关文档

- [安装指南](INSTALLATION_CN.md) - 如何安装和配置
- [架构说明](ARCHITECTURE_CN.md) - 系统设计和架构
- [使用示例](EXAMPLES_CN.md) - 更多实际示例
- [故障排除](TROUBLESHOOTING.md) - 常见问题解决

---

## 📞 获取帮助

如有问题，请：
1. 查看[故障排除文档](TROUBLESHOOTING.md)
2. 在[GitHub Issues](https://github.com/KALUSO-nolodjska/ai-team-mcp/issues)提问
3. 发送邮件至：lhq2328616309@outlook.com

---

**最后更新**: 2025-11-12  
**版本**: v5.0

